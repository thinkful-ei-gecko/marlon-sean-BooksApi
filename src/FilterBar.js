import React from 'react';
import ReactDOM from 'react-dom'

class FilterBar extends React.Component {

    render(){

        return(
            <section>
                <select>

                </select>
                <select>
                    
                </select>
            </section>
        )
    }
}